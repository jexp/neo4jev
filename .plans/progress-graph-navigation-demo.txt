## TASK-002 — 2026-09-16T00:42:29Z
Added src/neo4jev/config.py (frozen Settings dataclass loaded via python-dotenv, NEO4J_URI takes precedence over NEO4J_URL) with unit tests covering both var names, precedence, and missing-required-var errors.
Files: +src/neo4jev/config.py, +tests/unit/test_config.py
Decisions: .env.example could NOT be created — this sandbox hard-blocks Write/Edit/Bash on any `.env*` path (security guard, not a permission-ask). Someone with an unrestricted shell needs to add `.env.example` at repo root (content: NEO4J_URI, NEO4J_USERNAME, NEO4J_PASSWORD, NEO4J_DATABASE set to the public Companies KG demo values, plus a TYPESAFE_API_KEY placeholder) — tracked as an open gap against task-002's acceptance criteria.

## TASK-001 — 2026-09-16T00:43:16Z
Wrote project README.md covering purpose/architecture (TypeSafe system_one Choice+Noul beam search over a schema-agnostic Neo4j graph), setup (uv sync, .env.example -> .env with NEO4J_URL/URI, USERNAME, PASSWORD, DATABASE, TYPESAFE_API_KEY), usage for notebooks/ and app/streamlit_app.py, and the three goal modes, explicitly framed as forward-looking since no library code exists yet.
Files: +README.md, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: Removed an unverified external "typesafe.dev" link per review guidance; noted that Jupyter/ipykernel are not yet declared dependencies and suggested `uv run --with jupyterlab jupyter lab` until a later task adds them properly.

## TASK-003 — 2026-09-16T00:46:56Z
Added src/neo4jev/types.py as the pure shared contract (frozen dataclasses: NavCandidate, NavStep, NavPath, NavResult, BeamState, TerminationReason enum, and GoalSpec as a Literal-discriminated union of FreeTextGoal/TargetNodeGoal/PathIntentGoal) with zero neo4j/typesafe imports, plus 12 unit tests covering the discriminator, multi-path results, non-mutating BeamState.extend, frozen-field rejection, and import decoupling via AST scan.
Files: +src/neo4jev/types.py, +tests/unit/test_types.py
Decisions: Applied review feedback — added NavStep.candidates (all options considered, not just chosen; required by REQ-F-003, needed by the per-hop trace view) and NavCandidate.source_element_id/source_label (NavResult.neighborhood spans nodes from get_node_neighborhood(node_ids), so viz.py needs the relationship's source). Both were added at the END of each dataclass with defaults, so the field order the caller/PRD specifies stays intact for positional construction by concurrent task-004/005/006 authors. Kept the caller-specified field name `terminated_reason` over the reviewer's tense-nit rename (contract compatibility wins). Left frozen=True as shallow-frozen (dict/list fields are still mutable in place) and added a one-line comment on BeamState warning that it is not orderable/hashable, so navigator.py must use a unique tiebreaker in heapq keys.

## TASK-006 — 2026-09-16T01:06:47Z
Added src/neo4jev/viz.py: builds a neo4j_viz.VisualizationGraph from a NavResult with per-path coloring (path nodes/rels get palette color by path index, start node and neighborhood-only entities get their own colors and smaller/larger sizes), one shared legend, and two helpers — render_for_notebook() -> IPython HTML and render_for_streamlit() -> GraphWidget ready for neo4j_viz.streamlit.display_widget(). 15 unit tests (no network) assert entity counts, path-vs-neighborhood color/size distinctness, multi-path distinctness beyond palette size, dedupe of shared nodes/rels, and edge cases (empty result, zero-length path, unresolvable edge source).
Files: +src/neo4jev/viz.py, +tests/unit/test_viz.py, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: Roles are carried in a namespaced node/rel property so color_nodes/color_relationships can drive coloring and legend capture (as the task asks), then that key is popped before returning so the viz side panel shows only real Neo4j properties. Captions are heuristic (longest string property <=64 chars, else the node label, else the element id) to avoid hardcoding any property name (REQ-NF-003). Relationships are deduped by rel_element_id, first (lowest-index path) role wins, so a rel shared by two paths renders once. Review fixes applied: non-empty fallback source node for path steps, caption recomputed from accumulated props, dead sort-key tiebreaker removed, _colors/_legend collapsed, Theme renamed, and a test comment corrected.

## TASK-004 — 2026-09-16T01:09:10Z
Added src/neo4jev/neo4j_access.py: open_access() driver context manager, list_labels(), detect_indexes(label) (exact always; fulltext/vector only when SHOW INDEXES has a matching index), search_start_nodes(label, query, mode) for exact/fulltext/vector, get_outgoing_relationships() with per-type + total caps and "N of M edges considered" truncation metadata, get_node()/get_node_neighborhood(), plus pure helpers assign_edge_keys/cap_outgoing_edges/escape_lucene/default_embedder. 39 unit tests (fake driver, no network) and 9 integration tests, all green: 100 unit passed, 9 live integration passed against demo.neo4jlabs.com companies KG.
Files: +src/neo4jev/neo4j_access.py, +tests/unit/test_neo4j_access_mapping.py, +tests/integration/test_neo4j_companies_kg.py, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: Vector lookup defaults to a deterministic hash-seeded pseudo-embedding because no embedding-provider credentials exist (the server's genai plugin is unconfigured — genai.vector.encodeBatch fails with "'token' is expected to have been set"), documented in-code; a real `embedder` callable can be injected and the index dimension is read from SHOW INDEXES indexConfig. search_start_nodes takes an optional index_name because the live Chunk label has four vector indexes with three different dimensions. get_outgoing_relationships returns a list-like OutgoingCandidates (source id/label, candidates, by_type_totals/considered, .mapping edge_key->NavCandidate side table) since NavResult/NavStep own no capping metadata. Relationship types are visited in name order so capping is deterministic; per-type capping is applied in Cypher too, purely to bound transfer from supernodes (Apple has 2558 HAS_SUPPLIER edges). get_node_neighborhood keeps each relationship's real direction (source=startNode) and excludes edges internal to the given node set; NavCandidate has no source-property field, so only the target endpoint ships properties. neo4j_access has its own public settings_from_env() (Neo4j vars only) so this layer never requires TYPESAFE_API_KEY, and open_access() calls verify_connectivity() for a clear failure at session start. Review fixes applied: integration fulltext tie-order fragility (two Organization nodes named "Apple" share a score, so tests now pick from the top-5 hits rather than trusting limit=1), mode validated before the blank-query short-circuit, source_label fallback for edge-less nodes, optional index selection, and previously untested branches covered.

## TASK-005 — 2026-09-16T01:10:26Z
Added src/neo4jev/navigator.py: one system_one call per hop (Choice over opaque e0..eN edge keys mapped back to NavCandidate via a side table + Noul "goal reached?"), async beam search over AsyncTypeSafeClient with visited-node/max-depth/max-call guards and sum-of-log-prob scoring, all three GoalSpec modes (free_text single best path, target_node exact-id termination independent of Noul, path_intent stage decomposition + concurrent branches + top-N distinct paths). 30 unit tests (scripted fake client, plus an httpx2.MockTransport wire round-trip) and one live integration test that wires real neo4j_access + AsyncTypeSafeClient; 105 unit passed, integration skips cleanly (TYPESAFE_API_KEY empty in .env).
Files: +src/neo4jev/navigator.py, +tests/unit/test_navigator_scoring.py, +tests/integration/test_navigator_live.py, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: navigator takes an injected sync `CandidateFetcher` callable instead of importing neo4j_access, so it stays unit-testable with no driver (integration test wires the real one; verified live against the Companies KG: fulltext "Apple" -> 10 of 2772 capped edges -> e0..e9 -> 2 branches). A node with no outgoing relationships still gets its one call with the Noul alone, so a dead-end goal node is still recognizable (an empty Choice criteria map is invalid) — found by a failing test, not by inspection. Verified the request/response wire format end-to-end with a mock transport, which is now a unit test. Review fixes applied: integration-test skip guard now covers the lazy driver's first real query and TypeSafe errors, 255-option Choice backstop (MAX_CHOICE_OPTIONS), NavigatorConfig.__post_init__ validation (beam_width=0 previously returned an empty result silently), depth-capped states no longer double-recorded as BUDGET_EXHAUSTED, and tests for target_node ignoring a high Noul, the never-reached fallback, nested property serialization, and the option cap.

## TASK-007 — 2026-09-16T01:14:53Z
Added notebooks/01_explore_graph.ipynb: connects via open_access(), lists the live Companies KG's 10 labels, runs detect_indexes() over Organization/Person/Article/Chunk printing every FULLTEXT/VECTOR index with its properties and declared vector.dimensions (entity + news found), then makes one real search_start_nodes() call per mode — exact and fulltext on Organization "Apple", vector on Chunk via index_name="news", fulltext on Chunk — plus a cell showing Article's fulltext request failing loudly. Executed top-to-bottom via nbclient against the live graph with saved outputs: 0 errors, all 105 unit tests still green.
Files: +notebooks/01_explore_graph.ipynb, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: Vector sample passes index_name="news" explicitly (Chunk carries four vector indexes across 1536/768/384 dims) and its markdown states the default embedder is a deterministic placeholder, so the ranking is dimensionally real but not semantic. Markdown also warns that two Organization nodes are named exactly "Apple" and tie on fulltext score, so limit=1 is not deterministic for notebook 02. Review feedback applied: corrected the exact-mode narrative to name nodes actually returned (Simply Mac, via CONTAINS over every string property) instead of Apple (Germany), and scoped the "nothing hardcoded" claim to the library rather than the notebook.

## TASK-008 — 2026-09-16T01:18:18Z
Added notebooks/02_navigator_dry_run.ipynb (19 cells, nbclient-executed top-to-bottom with saved outputs): connects to the live Companies KG, picks a deterministic Organization start node from the two same-scoring "Apple" fulltext hits, shows the "60 of 2772 edges considered" capping with per-type breakdown, attempts exactly one real one_hop system_one call, and displays the probabilities dict / noul / top-k+cutoff selection.
Files: +notebooks/02_navigator_dry_run.ipynb, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: TYPESAFE_API_KEY is absent from the environment (Settings.from_env raises), so the one system_one call is attempted and its failure (TypeSafeAPIConnectionError: Illegal header value b'Bearer ') is displayed verbatim with a "fill TYPESAFE_API_KEY in .env and re-run this cell" note; nothing TypeSafe-shaped is faked. The top-k/cutoff logic still runs every time on an explicitly labelled synthetic uniform distribution over the hop's real e0..eN keys, and both branches verify the notebook's copy of the rule against navigator._select_branches on the same inputs (equivalence check therefore live even without a key). Review fix applied: notebook-local caption (longest non-URL string property) documented as such rather than claiming parity with viz.py's heuristic; empty-hits and zero-candidate guards added. Known gap: task-008's prose asks for "confidence", but HopResult (task-005) does not carry ChoiceAnswer.confidence, so a markdown cell documents that it is unavailable without a second call or a navigator change - candidate follow-up for task-011.

## TASK-010 — 2026-09-16T01:28:43Z
Added app/streamlit_app.py: label picker from list_labels(), a reusable lookup widget whose exact/fulltext/vector modes come from detect_indexes() per label (with an index dropdown when a label has several, e.g. Chunk's four vector indexes), the three goal modes with their own inputs (free text / second lookup widget + target-label picker / pattern textarea + top-N), sidebar run controls (top-k, cutoff, max depth, max call budget), a Run button driving navigator.navigate via asyncio.run + AsyncTypeSafeClient, the result rendered with the interactive neo4j_viz.streamlit.display_widget(viz.render_for_streamlit(result)), and a per-hop trace below it (one expander per path with Noul/log-prob/chosen edges plus an edge-key -> probability -> relationship table). Empty TYPESAFE_API_KEY gates gracefully (banner + clear error on Run) instead of raising.
Files: +app/streamlit_app.py, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: Results live in st.session_state as a (inputs-signature, NavResult) pair because the neo4j-viz widget reruns the whole script on interaction — the signature keeps a stale graph from being read as the current one, and a failed re-run clears it. Verified headlessly with streamlit.testing.v1.AppTest (scratch scripts in /tmp, not committed, since an AppTest keeper test would need live network and would break tests/unit's no-network rule): boot + label pick + exact/fulltext/vector search + all three goal modes + Run + reload-configuration, zero unhandled exceptions; a second AppTest run seeds a NavResult into session_state to exercise the display_widget + trace render path (real KG nodes, no TypeSafe output faked anywhere); `streamlit run` also boots and serves HTTP 200. Confirmed by experiment that asyncio.run works in Streamlit's script thread (no running loop) and that st.dataframe accepts a list of dicts, so the app needs no pandas import. Review fixes applied: NEO4J_ERRORS now also catches neo4j.exceptions.DriverError (verified ServiceUnavailable/ConfigurationError are DriverError siblings, NOT Neo4jError subclasses, so an unreachable server used to escape the handlers); dropped the unused `label` arg from _index_options; stale nav_result invalidation; generic (schema-agnostic) placeholder copy; vector-mode caption noting the placeholder embedder; AsyncTypeSafeClient construction moved inside the try; added a "Reload configuration" button because the cached settings/driver would otherwise keep reporting a freshly added key as missing until process restart. Not applied: the MEDIUM finding that _short_name duplicates viz.py's private caption heuristic — sharing it means editing library code from an app task, so the small duplication is deliberate. Open gap: task-010's criterion "manual run ... completes each of the three goal modes" could only be exercised in its error-gated form because TYPESAFE_API_KEY is empty; a real three-mode live run remains for whoever has a key (folds into task-011).

## TASK-009 — 2026-09-16T01:36:15Z
Added notebooks/03_full_traversal.ipynb (25 cells, nbclient-executed top-to-bottom with saved outputs): picks two deterministic looked-up start nodes (fulltext "Apple" Organization, exact-lookup Article "How Committed Is Big Blue To The IBM Cloud?"), then runs the full navigator.navigate beam search for all three goal modes — free_text ("find an organization that competes with Apple", max_depth 2 / 6 calls / beam 2), target_node (looked-up Google 4:14b688aa-...:6431, max_depth 3 / 8 calls) and path_intent (article -> MENTIONS -> Organization -> HAS_COMPETITOR -> Organization, max_depth 2 / 10 calls / top_n 3) — and renders each NavResult inline via viz.render_for_notebook, three HTML graphs with Start / Path N / Neighborhood in the legend, the path_intent one carrying three distinct path colors.
Files: +notebooks/03_full_traversal.ipynb, ~AGENTS.md, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: TYPESAFE_API_KEY is empty (same situation as task-008), so every section attempts the real run, displays TypeSafeAPIConnectionError verbatim, then drives the SAME real navigate() (real graph, capping, beam search, scoring, rendering) with a notebook-local KeywordStubClient that stands in for the model's answers and is labelled "STUB (not TypeSafe output)" on every line it produces — nothing TypeSafe-shaped is fabricated, but the honest consequence is that no real traversal is demonstrated and no run can terminate on goal_reached (the stub answers noul=0.0), which the "Known limitations" cell states. The stub also cannot serve target_node at all (navigator._candidate_criteria deliberately omits target_element_id, so no option text can match an id-based goal), and the section says so. path_intent uses the pattern "the organizations mentioned in the article -> the competitors of those organizations": the PRD's biomedical illustration (patient -> diseases -> genes -> expressions) cannot run on this graph, the notebook documents that adaptation in its own markdown cell, and it also records the direction constraint — navigator follows outgoing edges only and MENTIONS runs Article -> Organization, so the article must be the start node. Target selection intersects the lookup hits with the start node's capped out-neighbours and raises if that intersection is empty, making reachability a property of the cap rather than luck. AGENTS.md gained a Notebooks section because the size is a trap: viz render() inlines neo4j-viz's ~8 MB JS template per output, so this notebook is 25.2 MB on disk (~5.3 MB gzipped in git) and every re-execution rewrites the whole blob. Review feedback applied: the "Colors" narrative rewritten for the new viz.py semantics and made color-agnostic (a concurrent task-006 revision was changing START_COLOR/PATH_COLORS mid-review); added the goal_reached/limitation bullets; documented the cap coupling in target_node; noted that the hardcoded label names are the notebook's, not the library's. Not re-executed after the markdown-only edits, deliberately: the renders on disk come from the committed viz.py (4c84613) while an uncommitted, in-flight viz.py colour change was sitting in the working tree. Open gap for task-011: re-execute 03 with TYPESAFE_API_KEY present so at least one "source: live TypeSafe" line exists per goal mode.

## TASK-011 — 2026-09-16T01:55:53Z
Final wrap-up: full-suite pass, companies2 migration repair, PRD acceptance verification, docs.
Discovered companies2 schema (live SHOW INDEXES / db.labels(), server neo4j 2026.05.0 enterprise): 15 labels — Article, CPCClass, Chunk, City, Country, IPCClass, IndustryCategory, Investment, NAICSCode, Organization, Patent, Person, Region, SECFiling, Technology (568315 nodes / 2530106 rels). FULLTEXT: `organization_fullName` on Organization.fullName, `person_name` on Person.name. VECTOR: `news_openai_small` on Chunk.embedding_3_small (1536 dims, COSINE, HNSW m=16, quantization on). There is NO `entity` fulltext index and NO `news` vector index — those belong to the old `companies` db; `Article` has neither (only exact), and `MENTIONS` runs Article -> Organization (24527 edges). `Organization` has both `name` and `fullName`, and the fulltext index covers `fullName` only, so fulltext "Apple" returns Apple Music / Apple Ads / Apple Inc. (all tied at 4.223) while exact "Apple" returns the three orgs actually named Apple.
Files: ~tests/integration/test_neo4j_companies_kg.py, ~src/neo4jev/navigator.py, ~tests/unit/test_navigator_scoring.py, ~notebooks/01,02,03, ~README.md, ~AGENTS.md, ~.plans/prd-graph-navigation-demo.md, ~.plans/tasks-graph-navigation-demo.yml
Decisions: (1) Integration tests updated to companies2's real indexes (EXPECTED_FULLTEXT_INDEX organization_fullName + EXPECTED_FULLTEXT_PROPERTY fullName, EXPECTED_VECTOR_INDEX news_openai_small, EXPECTED_VECTOR_DIMENSIONS 1536); the Chunk vector assertion now pins the actual dimension instead of "> 0", so it would fail loudly if the index moved. (2) task-008's flagged follow-up ASSESSED AND DONE — it was the small clean fix predicted: HopResult gained a trailing `confidence: float | None = None` populated from ChoiceAnswer.confidence, so nothing else needed touching (HopResult is only constructed in navigator.py and field-accessed nowhere in the suite). Two unit assertions added: 0.8 threaded through in the httpx2 wire round-trip test, and None when a node has no outgoing edges (no Choice asked => no Choice confidence). Notebook 02 now displays it in both the hop summary and the full-probabilities cell, and notebook 03's "HopResult does not carry confidence" limitation bullet was rewritten to say the beam search deliberately ignores it in sum(log(p)) scoring. (3) Notebook 01: the old 3d "fulltext on Chunk" section could not survive (companies2 Chunk has no fulltext index), so it now demonstrates the graph's *other* fulltext index, `person_name`, via a "Tim Cook" lookup; the vector sample moved to index_name="news_openai_small"; summarize() now prints a readable caption (name/fullName/title, else longest string) so the markdown claims are checkable against the output instead of being hidden behind arbitrary first-3-properties ordering. (4) Notebook 03 had TWO unresolvable references, not one: the Article title (now "Stocks to Watch: Infosys, Coal India, Vedanta, Paytm, HDFC Bank, Yes Bank, Axis Bank", 3 MENTIONS so top_n=3 has headroom) AND the target node — no "Google" fulltext hit is a capped out-neighbour of Apple, so the whole Organization start moved from Apple to Google. That was forced by a real property of this dataset, not by taste: cap_outgoing_edges visits relationship types in NAME order and then trims to total_cap, so Apple Inc.'s 1354 edges spend the entire 60-edge budget on APPLIED_FOR..HAS_CATEGORY and HAS_COMPETITOR reports "0 of 26" — the free_text goal "find an organization that competes with Apple" is unreachable in one hop from Apple. Google's cap does include HAS_COMPETITOR (Apple Inc./Amazon/Microsoft/…), so both free_text and target_node now run from Google, the target node being Microsoft (:10), picked as the lookup hit that is also a capped out-neighbour. Notebook 03's before/after: 3 MENTIONS -> 3 distinct paths, and the free_text run visibly takes HAS_COMPETITOR -> Samsung Electronics. (5) Regenerated 01/02/03 top-to-bottom against companies2 via nbclient with a kernelspec pointing at .venv; 0 error outputs; 03 keeps its 3 inline HTML renders (25.6 MB, expected). (6) README/AGENTS/PRD drift fixed: Status section no longer claims the files "do not exist yet", "Planned modules" -> "Modules", all companies->companies2 refs, a "What the demo graph offers" table plus the cap-ordering caveat in README, and a "Target Dataset (companies2)" section in AGENTS.md carrying the index layout and the alphabetical-cap gotcha; the PRD got a factual "mid-build change" note (requirements untouched — they are schema-agnostic by design).
PRD Acceptance Criteria verification (task-011, re-read .plans/prd-graph-navigation-demo.md): 1 README setup/usage for notebooks + app — SATISFIED (updated; documents uv sync, .env vars, notebook order, streamlit run, and the key-gated stand-in behaviour). 2 `uv sync && pytest tests/unit` zero network — SATISFIED, 106 passed in ~0.8s. 3 `pytest tests/integration` against the live KG — SATISFIED, 9 passed (the live TypeSafe test skips cleanly while TYPESAFE_API_KEY is empty, which is its documented contract). 4 all three notebooks top-to-bottom, 03 visibly showing all three goal modes with rendered graphs — SATISFIED, 0 error outputs, 03 carries 3 inline HTML renders. 5 `streamlit run app/streamlit_app.py` allows label pick + per-mode start-node search + all three goal modes + run + interactive graph — SUBSTANTIALLY VERIFIED, ONE ITEM UNVERIFIABLE HERE: HTTP 200, and an AppTest run confirms the label picker offers all 15 companies2 labels and the Lookup-mode radio auto-detects per label exactly as detect_indexes reports (Organization -> exact+fulltext, Chunk -> exact+vector, Article -> exact), all three goal modes are offered with no exceptions; but "running a traversal" cannot be exercised while TYPESAFE_API_KEY is empty (the app gates it with a banner and a clear error, as task-010 recorded). 6 path_intent returns >1 distinct path — SATISFIED FOR THE PIPELINE, NOT FOR LIVE TYPESAFE: the executed notebook returns 3 distinct paths, driven by the labelled stub because no key exists. 7 no hardcoded label/rel-type/property in src/neo4jev/ — SATISFIED, verified by grep (the only match is record["name"], a SHOW INDEXES result key). HONEST GAP, NOT WORKED AROUND: acceptance criteria 5 and 6 are only fully demonstrable with a real TYPESAFE_API_KEY, so AC 5 is "5 of 6 controls live-verified" and AC 6 is "3 distinct paths under the stub, 0 live runs". PRD checkboxes deliberately left unticked rather than ticked on a partial verification.
Review feedback applied (hone-reviewer; no CRITICAL/HIGH findings). MEDIUM: the vector integration test kept the old index name in its function name — renamed to test_detect_indexes_finds_news_openai_small_vector_index_for_chunk with the local renamed news->vector, so the name no longer describes an index that does not exist here. LOW: the vector test now asserts index-name membership and kind=="VECTOR" before the next(), so a missing/renamed index fails as a clear assertion instead of StopIteration (matching the fulltext test); notebook 02's cap table prose no longer reads as if it listed every zero-contribution type (there are 10, not 4); AGENTS.md's description of the old companies `entity` index corrected to "over Person/Organization name" (it was never Organization-only); README no longer implies notebook 02 runs a beam search. Not applied: extending confidence into NavStep/NavResult — task-008 asked for it on the single-hop result the dry-run notebook consumes, and it intentionally plays no part in sum(log(p)) scoring, so HopResult is the right boundary; recorded here as a deliberate stop rather than an oversight.

## REVIEW — 2026-09-16T02:47:48Z
Added 4 review tasks (task-012..task-015).

## TASK-014 — 2026-09-16T02:54:10Z
Collapsed the duplicated positional edge-keying into ONE shared helper and dropped the never-consumed tuple return.
Files: ~src/neo4jev/types.py, ~src/neo4jev/neo4j_access.py, ~src/neo4jev/navigator.py, ~tests/unit/test_types.py, ~tests/unit/test_neo4j_access_mapping.py
Decisions: The helper went to types.py next to NavCandidate (the task's preferred home) as `assign_edge_keys(candidates) -> list[NavCandidate]` — it needs only dataclasses.replace + NavCandidate, so types.py stays SDK-free/dependency-free and its AST import-guard test still passes; the single rationale comment (positional keys because same-typed rels to different targets would otherwise collapse into one option) now lives ONCE on that helper's docstring, and the duplicate copies in neo4j_access.py and navigator.py are deleted with their functions. neo4j_access no longer defines or re-exports the name — nothing else referenced neo4j_access.assign_edge_keys (verified repo-wide across src/, tests/, notebooks/, app/), so a thin alias would have been dead weight; it imports the shared helper instead. The tuple return is gone: both neo4j_access call sites are now plain `keyed = assign_edge_keys(...)` / `return assign_edge_keys(candidates)`, and OutgoingCandidates.mapping remains the single side-table (it is a property over candidates, untouched, and still asserted in the mapping tests). navigator._key_candidates is deleted; one_hop now calls the shared helper on the MAX_CHOICE_OPTIONS slice. The now-unused `replace` dataclasses import was removed from both modules (`Sequence` is still used in both, so it stayed). Tests: the two old assign_edge_keys tests (which asserted the tuple's mapping half) moved from test_neo4j_access_mapping.py to test_types.py beside the helper and were adapted to the list-only contract — coverage for the moved assertions is not lost because OutgoingCandidates.mapping["e0"]/["e10"] round-trip is still asserted in test_neo4j_access_mapping.py. `uv run pytest tests/unit -q` -> 113 passed in 0.84s, no network.
Review feedback applied (hone-reviewer; verdict LGTM, no CRITICAL/HIGH/MEDIUM). LOW: the new test's inline comment restated the helper docstring's rationale verbatim, violating the task's one-rationale-comment constraint — deleted; `test_assign_edge_keys_leaves_input_untouched_and_of_empty_list` bundled two unrelated assertions and only checked the input side of immutability — split into test_assign_edge_keys_of_empty_list_returns_a_list and test_assign_edge_keys_leaves_the_input_candidates_untouched, the latter now asserting both keyed[0].edge_key == "e0" and original.edge_key == "stale-key". Not applied: the reviewer's informational note that one_hop still builds a local by_key dict — that is a pre-existing, hop-local derivation needed because one_hop re-keys after slicing to MAX_CHOICE_OPTIONS, not a duplicate of OutgoingCandidates.mapping; and the ruff-format note, since the repo has no formatter configured and the flagged lines are pre-existing.

## TASK-012 — 2026-09-16T02:54:37Z
Synced docs and notebooks with the round-robin total cap: README + AGENTS rewritten, notebooks 02/03 re-executed against companies2.
Files: ~README.md, ~AGENTS.md, ~notebooks/02_navigator_dry_run.ipynb, ~notebooks/03_full_traversal.ipynb, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: (1) Live-verified the new figures before writing them: Apple Inc. 1354 outgoing edges -> 60 considered across ALL 17 relationship types (1-4 edges each, min is HAS_CEO at 1 of 1), HAS_COMPETITOR 4 of 26 (Amazon e8 / Microsoft e24 / Samsung e40 / Nokia e55); Google 60 of 1488 -> HAS_COMPETITOR 4 of 48. Both stale claims ("fills alphabetically", "HAS_COMPETITOR contributes nothing", "0 of 26") are gone from README, AGENTS and notebook 02's md cell 6; the notebook-02 cell now explains the two-pass cap (per-type trim first, then round-robin one-edge-per-type passes) and keeps the still-true "the cap only samples a supernode" caveat instead of the false starvation one. (2) Notebook 03's start node stays Google: I checked whether the round-robin change lets Apple work again, and it does — Microsoft (:10) is a capped out-neighbour of Apple Inc. just as it is of Google — so the old forced move is now a free choice. Kept Google rather than churning the notebook back to Apple, and rewrote md cell 4 to say exactly that (both start nodes now keep their HAS_COMPETITOR share; Google is kept because notebook 02 already starts from Apple and repeating it would make the two notebooks the same run twice), so the rationale no longer rests on a starvation fact that is now false. (3) Execution: nbclient + ipykernel installed into .venv and a /tmp kernelspec, allow_errors=False, execution timestamps stripped from cell metadata (matches the existing convention). 0 error outputs in both; 02's saved per-type table now shows "4 of 26 HAS_COMPETITOR edges considered" (was "0 of 26"), 03 keeps its three inline renders at ~26 MB. Branch picks did shift as expected: free_text now goes HAS_COMPETITOR -> Amazon.com then HAS_CUSTOMER -> Instagram (was HAS_COMPETITOR -> Samsung before); path_intent still returns 3 distinct paths (MENTIONS -> HAS_COMPETITOR x3). (4) REQUIRED SIDE EFFECT, not in the task text: notebook 02/03 setup cells called neo4j_access.settings_from_env(), which the concurrently-running task-013 deleted in favour of Settings.from_env(require_typesafe_key=False) — the notebooks could not execute at all without that edit, so both code cells and their setup markdown were migrated (notebooks are this task's file scope, so no conflict with the parallel agents). (5) TYPESAFE_API_KEY gating untouched: key still empty, all three notebook-03 sections still print "source : STUB (not TypeSafe output)", 02 still displays the TypeSafeAPIConnectionError verbatim. Verified 112 unit tests pass. hone-reviewer found no CRITICAL/HIGH/MEDIUM; applied its one worthwhile LOW (dropped the literal commit hash from the AGENTS.md pivot sentence) and left the LOW about README/AGENTS/notebook prose overlap alone, since each surface has to stand alone for its own reader.

## TASK-013 — 2026-09-16T02:55:12Z
Collapsed the duplicated env loading into config.py: neo4j_access no longer owns a second loader.
Files: ~src/neo4jev/config.py, ~src/neo4jev/neo4j_access.py, ~tests/integration/test_neo4j_companies_kg.py, ~tests/unit/test_config.py, ~tests/unit/test_neo4j_access_mapping.py, ~app/streamlit_app.py, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: Settings.from_env gained the keyword-only variant flag `require_typesafe_key: bool = True`; with False it takes `os.environ.get("TYPESAFE_API_KEY", "")` instead of `_require_env`, which is byte-equivalent to what neo4j_access.settings_from_env used to do, so the env contract (NEO4J_URI precedence, the "Missing required environment variable: NEO4J_URI (or NEO4J_URL)" message, bare `load_dotenv()` == `load_dotenv(dotenv_path=None)`) is unchanged. neo4j_access.settings_from_env, _require_env and the now-unused os/load_dotenv imports are deleted; open_access() falls back to Settings.from_env(require_typesafe_key=False) and still accepts a plain Settings. The task's file list named only the integration test, but removing the helper had three more callers that would have broken: tests/unit/test_neo4j_access_mapping.py (two settings tests moved to test_config.py, where the require_typesafe_key=False cases now live alongside the existing strict-mode ones) and app/streamlit_app.py (get_settings() -> Settings.from_env(require_typesafe_key=False); behavior identical, it still gates on `bool(settings.typesafe_api_key.strip())`). notebooks/02 and 03 still call `neo4j_access.settings_from_env()` in a code cell — deliberately NOT touched (task-012 was re-executing those exact notebooks concurrently and owns them); they need `Settings.from_env(require_typesafe_key=False)` before their next execution, flagged to the caller rather than edited across an active task boundary. Review feedback applied (hone-reviewer; verdict LGTM, no CRITICAL/HIGH/MEDIUM). LOW (applied): nothing asserted the behavior the refactor exists to preserve, so test_open_access_without_settings_tolerates_a_missing_typesafe_key was added to test_neo4j_access_mapping.py — monkeypatches the driver factory and neo4jev.config.load_dotenv, deletes TYPESAFE_API_KEY, and asserts open_access() with no arguments still opens (the regression it guards is re-introducing a hard key requirement for graph-only callers). Not applied: the LOW note that `typesafe_api_key=""` conflates unset with explicitly-empty (Optional[str] would break the preserved field contract and the single consumer already guards on the empty string), and the LOW suggestion to add a tmp_path .env-loading test (the env_file path is already exercised via "/nonexistent", and a real-.env test would have to re-test python-dotenv itself). `uv run pytest tests/unit -q` -> 114 passed in 0.82s (no network); `uv run pytest tests/integration -q` -> 9 passed, 1 skipped (the TYPESAFE_API_KEY-gated live TypeSafe test) against companies2.

## TASK-015 — 2026-09-16T02:55:03Z
Collapsed the six GoalSpec isinstance cascades in src/neo4jev/navigator.py into one per-hop `GoalView` (frozen dataclass: mode, description, noul_true, stages, stage_index, target_element_id, top_n) built by a single new dispatch point `goal_view(goal, hop_index=0)`; deleted `_stages_for`, `_goal_description`, `_noul_true_description`, `_choice_instructions`, `_goal_reached_by_noul`, `_select_result_paths`, both `assert stages is not None` guards, and the four-wide (goal, stages, stage_index) threading into `_node_state`. The four textual concerns are now view fields (`description`, `noul_true`, `view.choice_instructions()`), and the two decision functions hang off the view (`view.goal_reached_by_noul(noul, threshold)`, `view.is_target(element_id)`, `view.select_result_paths(terminated, leftover)`). `isinstance(goal, ...)` now appears only inside `goal_view`; `navigate`'s start-node check uses `view.is_target` too. types.py untouched.
Files: ~src/neo4jev/navigator.py, ~.plans/tasks-graph-navigation-demo.yml, ~.plans/progress-graph-navigation-demo.txt
Decisions: (1) Behavior-neutrality verified two ways — `tests/unit/test_navigator_scoring.py` is unmodified from HEAD and all 37 of its tests pass, and a direct spot-check of `goal_view` for all three modes reproduces the old strings byte-for-byte (Choice instructions "Goal: {desc}" vs the path-pattern branch, Noul "true" criterion, `_node_state.goal` mode/description/stages/current_stage_index/current_stage, stage clamping at hop_index=7). (2) Sentinel discrimination: `target_element_id is not None` ⇔ target_node and `top_n is not None` ⇔ path_intent, both safe because types.py makes those fields required (`TargetNodeGoal.target_element_id: str`, `PathIntentGoal.top_n: int = 3`); the only divergence is a type-invalid `top_n=None`, which the old code raised TypeError on and the new code treats as single-best (more robust, not a regression). (3) To get exactly ONE view build per hop, `_expand` builds the view and calls a new private `_execute_hop(client, node, candidates, view, ...)` that consumes it, while the public `one_hop(..., goal, ...)` (signature unchanged — tests call it positionally) builds the view and delegates; without that split `_expand` would have to rebuild the view for its post-call termination checks. (4) hop_index was left out of the shared view builder name by keeping `goal_view(goal, hop_index=0)` defaulted, so `navigate` can use the same builder for the hop-independent target check and result-path selection; a one-line comment marks that only hop-independent fields are used there.
Review feedback applied (hone-reviewer; one HIGH, two LOW). HIGH: the refactor orphaned the `PathIntentGoal` import (the path_intent branch is reached by union fallthrough, not isinstance) — removed it, so `uv run ruff check src/neo4jev/navigator.py` is clean. LOW (applied): both new public names `GoalView`/`goal_view` were missing from `__all__` — added (chose "intended API" over the private-name alternative because task-015 names `goal_view` explicitly). LOW (applied): a comment at `navigate`'s view construction noting only the hop-independent fields (target identity, path selection) are used there, closing the trap where a reader could read hop-0 `stages`/`stage_index` at that level. Not applied: nothing else raised. `uv run pytest tests/unit -q` -> 114 passed in 0.81s (no network; the extra 2 over task-011's 106 are concurrent task-013/014 tests).
Note for the caller: navigator.py also carries concurrent task-014 changes (`_key_candidates` removed, `assign_edge_keys` now imported from types.py), so this commit stages the whole file and will include them.

## TASK-012 PROVENANCE — 2026-09-16T02:54:37Z
The task-012 changeset (README.md, AGENTS.md, notebooks 02/03, the task-012 entry in the tasks YAML, and the TASK-012 entry above) landed inside commit `39d7934` ("graph-navigation-demo-task-014: deduplicate edge-keying into one shared helper"), not a task-012 commit of its own. Cause: concurrent agents stage into one shared git index, and task-014's `git commit` (no pathspec) committed everything that happened to be staged at that instant — its own files, task-015's navigator.py, and task-012's staged files. No amend/rebase/reset was performed, so the content is intact and verified (README/AGENTS round-robin prose, notebook 02 md cell 6, task-012 `completed_at: 2026-09-16T02:54:37Z`, 0 error outputs in both notebooks) but its provenance is this commit and not the message it is filed under. Worth avoiding in future fan-outs: `git commit -- <paths>` scopes the commit to named paths and leaves other agents' staged work in the index.

## REVIEW — 2026-09-17T20:02:45Z
Added 1 review task (task-016): per-label display-property identity.
